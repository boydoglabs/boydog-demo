{
  "name": "sharedb-example-textarea",
  "version": "0.0.1",
  "description": "A simple client/server app using ShareDB and WebSockets",
  "main": "server.js",
  "scripts": {
    "start": "forever --verbose --uid \"boydog-remake-demo\" -w ./server.js",
    "build": "browserify client-library.js -o public/boydog-client.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "BoyDog",
  "contributors": [
    "BoyDog"
  ],
  "license": "MIT",
  "dependencies": {
    "express": "^4.14.0",
    "reconnecting-websocket": "^3.0.3",
    "sharedb": "^1.0.0-beta",
    "sharedb-string-binding": "^1.0.0",
    "websocket-json-stream": "^0.0.1",
    "ws": "^1.1.0"
  },
  "devDependencies": {
    "browserify": "^13.0.1"
  }
}
